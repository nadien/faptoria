var index = module.exports = {
    Middleware: require('./middleware'),
    EscapedFragmentUtilities: require('./escaped-fragment-utilities'),
    HtmlSnapshot: require('phantom-html-snapshot'),
    SearchEngineDetector: require('./search-engine-detector')
};

middleware = index.Middleware(index.HtmlSnapshot, index.SearchEngineDetector, index.EscapedFragmentUtilities);

module.exports.middleware = middleware.execute;
